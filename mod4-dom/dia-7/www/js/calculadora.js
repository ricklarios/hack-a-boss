'use strict';

const form = document.forms;
const numeroA = Number(form.elements.numA.value);
const numeroB = Number(form.elements.numB.value);
const operation = form.element.operadores.value;

console.log(operation);

const operator = (numeroA, numeroB, operation) => {};
